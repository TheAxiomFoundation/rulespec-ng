# Declare Nigeria-specific helper variables here when an encoded rule needs
# a reusable concept that is not directly present in source law. Individual
# income tax is federal law applied uniformly, so all helpers are national
# (ng) scope.
